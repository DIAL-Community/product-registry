var esriApi;
var basePath;
document.addEventListener('turbolinks:load', function() {
  esriApi = "<%= Rails.configuration.geocode["esri"]["suggest_uri"] %>";
  basePath = "<%= Rails.env.production? ? '/admin' : '' %>"
});
