<p id="notice"><%= notice %></p>
<div class="card">
  <h5 class="card-header bg-secondary text-white">
    Location: <%= @location.name %>
  </h5>
  <div class="card-body">
    <p class="float-right mb-0" >
      <%= link_to 'Back', locations_path, :class => "card-link" %>
      <%= link_to 'Edit', edit_location_path(@location), :class => "card-link" %>
    </p>
  </div>
</div>

<div class="mt-4">
  <div class="border rounded bg-secondary text-white clearfix border card-header">
    <h5 class="float-left mb-0">Organization</h5>
    <%= link_to 'Add', new_organization_path, :class => "card-link text-white float-right" %>
  </div>
  <% if @location.organizations.count > 0 %>
    <div class="row">
      <% @location.organizations.each do |organization| %>
        <div class="col-6 col-lg-4 mt-2">
          <div class="card h-100">
            <div class="card-body pb-1">
              <h5 class="card-title"><%= link_to organization.name, organization %></h5>
              <h6 class="card-subtitle text-muted mb-2"><%= organization.website %></h6>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="card-text text-muted">We don't have any organization(s) information for this location.</p>
  <% end %>
</div>
