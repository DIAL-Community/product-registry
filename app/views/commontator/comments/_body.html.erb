<%#
  Views that use this partial must provide the following variable:
  comment
%>

<div id="quill-<%= comment.id %>" body-id="quill-body-<%= comment.id %>" class="wysiwyg-viewer">
  <%= comment.body.html_safe %>
</div>
