<% if @page.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t('view.general.error', count: @page.errors.count) %>
    </div>
  </div>
<% end %>
<h5 class="border rounded bg-secondary text-white p-3">
  <%= t('view.general.title.edit', model: t('model.playbook-page')) %>
</h5>
<%= render 'form', playbook_page: @page, page_contents: @page_contents, pages: @pages %>
