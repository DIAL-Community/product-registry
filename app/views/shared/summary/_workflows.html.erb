<%
  hidden_class = 'd-none d-md-block'
  if (context == 'top-summary' && index < max_nav) ||
     (context == 'drop-summary' && index >= max_nav)
    hidden_class = 'd-block'
  end
%>

<div class="list-group-item w-100 text-center bg-transparent <%= hidden_class %>">
  <%= link_to workflows_path do %>
    <%
      btn_class = 'btn-secondary'
      if current_page?(:controller => 'workflows', :action => 'index')
        btn_class = 'btn-warning'
      end
    %>
    <span class="w-100 btn <%= btn_class %>">
      <div class="inline" data-toggle="tooltip" data-placement="bottom" data-original-title="<%= t('model.workflow').titlecase.pluralize %>">
        <%= image_tag "workflows_icon.png", :width => "20px", :height => "20px",
                    alt: t('alt.el-icon', el: t('model.workflow')).humanize %>
      </div>
      <span class="display-text-on-xl">
        <%= t('model.workflow').titlecase.pluralize %>
      </span>
      <span class="workflow-badge badge badge-light">--</span>
    </span>
  <% end %>
</div>
