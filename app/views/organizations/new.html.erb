<% if @organization.errors.any? %>
  <div id="error_explanation">
    <div id="error_explanation">
      <% if @organization.errors.include?(:duplicate) %>
        <div class="alert alert-warning">
          The form contains
          <%= pluralize(@organization.errors.count, "warning") %>.
        </div>
      <% else %>
        <div class="alert alert-danger">
          The form contains
          <%= pluralize(@organization.errors.count, "error") %>.
        </div>
      <% end %>
    </div>
  </div>
<% end %>
<div class="card">
  <h5 class="card-header">
    Create organization
  </h5>
  <%= render 'form', organization: @organization %>
</div>
