<% if @task_tracker.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t('view.general.error', count: @task_tracker.errors.count) %>
    </div>
  </div>
<% end %>
<div class="border rounded bg-secondary text-white p-3">
  <%= t('view.general.title.create', model: t('model.task-tracker')) %>
</div>
<%= render 'form', task_tracker: @task_tracker %>
